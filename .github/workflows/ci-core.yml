name: ci-core

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  build_test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: main # TODO remove once temp build step below is removed

      - name: Setup rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      # This is a temporary build step that clones another
      # repository as a private un-published dependency 'blstrs'
      # it uses a GH personal access token with read only permission
      - name: Check out mattrglobal/blstrs
        uses: actions/checkout@master
        with:
          repository: mattrglobal/blstrs
          token: ${{ secrets.MATTR_BLSTRS_PAT }}
          path: blstrs # TODO remove once temp build step below is removed

      - name: Run cargo check
        uses: actions-rs/cargo@v1
        working-directory: ./main # TODO remove once temp build step below is removed
        with:
          command: check

      - name: Run cargo audit
        uses: actions-rs/cargo@v1
        working-directory: ./main # TODO remove once temp build step below is removed
        with:
          command: audit

      - name: Run cargo test
        uses: actions-rs/cargo@v1
        working-directory: ./main # TODO remove once temp build step below is removed
        with:
          command: test
          args: "--release"

      - name: Run cargo build
        uses: actions-rs/cargo@v1
        working-directory: ./main # TODO remove once temp build step below is removed
        with:
          command: build

  fmt-clippy:
    name: Rust Format & Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          path: main # TODO remove once temp build step below is removed

      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: rustfmt, clippy

      # This is a temporary build step that clones another
      # repository as a private un-published dependency 'blstrs'
      # it uses a GH personal access token with read only permission
      - name: Check out mattrglobal/blstrs
        uses: actions/checkout@master
        with:
          repository: mattrglobal/blstrs
          token: ${{ secrets.MATTR_BLSTRS_PAT }}
          path: blstrs # TODO remove once temp build step below is removed

      - uses: actions-rs/cargo@v1
        working-directory: ./main # TODO remove once temp build step below is removed
        with:
          command: fmt
          args: --all -- --check

      - uses: actions-rs/cargo@v1
        working-directory: ./main # TODO remove once temp build step below is removed
        with:
          command: clippy
          args: -- -D warnings
